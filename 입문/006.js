// 006.js
(3 + 12) / 5;
declaredVariable;
greeting("Hello");

// 006-1.js
if (true) {
}

// 006-2.js
// greeting 함수 정의
function greeting() {
  "hello";
  "Chloe" + 3;
  greeting();
}

// 함수 인자에는 값을 넣어야 하는데 값 대신 if 명령문을 넣어 잘못된 문장이므로 구문 오류 발생
// greeting(if(true) { console.log("It is not acceptable") })
